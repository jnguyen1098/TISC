#include <assert.h>
#include <stdio.h>
#include <stdlib.h>

#include "TISC.h"
#include "TISC_defs.h"

// TODO(jason) header?
void run_all_tests(int level);

void run_all_tests(int level)
{
    if (level) {}
}
